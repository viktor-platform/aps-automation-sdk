interface:
  display_name: "APS ACC Public Activity Signing"
  short_description: "Power workflow for APS public activity signing"
  default_prompt: "Use $aps-acc-public-activity-signing to run full signing workflow: validate env/token, set nickname, generate/export keys, upload public key, sign activity ID, and map signature to WorkItemAcc public execution."
